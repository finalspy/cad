{
  "name": "cours",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "live-server --open=./index.html --port=8000",
    "build": "pnpm exec reveal.js build index.html dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "reveal.js": "^5.2.1"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
